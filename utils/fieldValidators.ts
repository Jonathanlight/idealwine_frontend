export const usernamePattern = /^[a-zA-Z0-9._-]{4,}$/;
export const emailPattern = /^\S+@\S+$/;
export const passwordPattern = /^(?=.*[A-Za-z])(?=.*[0-9]).{12,}$/; // at least 12 characters with at least one letter and one number
